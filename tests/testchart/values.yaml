replicaCount: 1

image:
  repository: nginx
  tag: stable
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

secret: "{{ssm jessica-secret}}"

contexts:
- "{{ssm-path /configmgmt/jessica_first_context}}"
- "{{ssm-path /configmgmt/jessica_second_context}}"

